const title = {
    fontWeight: "bold",
}

const shopText = {
    color: "orange"
}

const container = {
    alignItems: "center",
    color: "blackAlpha.700",
    zIndex: 1,
    w: '100%'
}

export {title, shopText, container};